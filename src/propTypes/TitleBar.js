import {
  string
} from 'prop-types'

export const defaultProps = {
  titleText: 'Default title',
  description: 'Default description'
}

export default {
  titleText: string,
  description: string
}
